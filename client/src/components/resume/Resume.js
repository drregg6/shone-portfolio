import React from 'react';
import PropTypes from 'prop-types';

const Resume = props => {
  return (
    <div>
      <h1>Resume</h1>
    </div>
  )
}

// Resume.propTypes = {

// }

export default Resume;